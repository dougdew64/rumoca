# TwoLoops — why this specimen exists

*What this specimen is for, and which of Doug's questions it has answered.
**Not** a walkthrough of its trip through the pipeline: that prose was retired
2026-07-29 because Claude regenerates it on demand, and a stored copy goes stale
without anything checking it. See `docs/ideas.md` #42.*

**Ground truth is [`trace/`](trace/)** — the per-stage IR, generated by
`cargo run -p hrw --example gen_trace -- TwoLoops`, with the Rumoca rev and
specimen hash stamped in [`trace/manifest.json`](trace/manifest.json). Any number
about this specimen should be read from there, never from prose.

---

## Why this specimen exists

`TwoLoops` ([`specimens/TwoLoops.mo`](../../../specimens/TwoLoops.mo)) chains two
idealized proportional loops: the first loop's controller output drives the second
loop's setpoint.

```modelica
errorA   = reference - plantA * commandA;   // ┐ loop A
commandA = gainA * errorA;                  // ┘
errorB   = commandA - plantB * commandB;    // ┐ loop B  (driven by commandA)
commandB = gainB * errorB;                  // ┘
```

It exists to show that a system can contain **several independent simultaneous
blocks**. Loop A depends on nothing but the reference; loop B depends on loop A
but not vice versa. So structural analysis finds **two** strongly-connected
components — two coupled blocks — and schedules them in order. Where
[`ProportionalLoop`](../ProportionalLoop/purpose.md) is one orange box and
[`MixedLoop`](../MixedLoop/purpose.md) is one box flanked by scalars, this is
**two boxes**.

---

## Questions this specimen answered

*Append here as it happens: the date, the question, and what made it click. This
is the half that cannot be regenerated, and it is what tells a later session
where Doug actually is. See `docs/ideas.md` #41.*

_(none recorded yet)_
