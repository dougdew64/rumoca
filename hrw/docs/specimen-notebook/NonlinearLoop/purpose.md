# NonlinearLoop — why this specimen exists

*What this specimen is for, and which of Doug's questions it has answered.
**Not** a walkthrough of its trip through the pipeline: that prose was retired
2026-07-29 because Claude regenerates it on demand, and a stored copy goes stale
without anything checking it. See `docs/ideas.md` #42.*

**Ground truth is [`trace/`](trace/)** — the per-stage IR, generated by
`cargo run -p hrw --example gen_trace -- NonlinearLoop`, with the Rumoca rev and
specimen hash stamped in [`trace/manifest.json`](trace/manifest.json). Any number
about this specimen should be read from there, never from prose.

---

## Why this specimen exists

`NonlinearLoop` ([`specimens/NonlinearLoop.mo`](../../../specimens/NonlinearLoop.mo))
is [`ProportionalLoop`](../ProportionalLoop/purpose.md) with one change: the plant
is **nonlinear** in the loop variable.

```modelica
error       = reference - measurement;
command     = controllerGain * error;
measurement = plantGain * command * command;   // command², not command
```

It exists to isolate a point that is easy to state and easy to forget: **structural
analysis is blind to nonlinearity.** The incidence matrix only asks *which unknowns
appear in which equation* — not how. So `command²` and `command` have the *same*
incidence, and the matching / BLT / tearing are **identical** to the linear loop.
The difference is entirely *numerical*, and it surfaces only once a solver tries to
close the torn loop — which makes this specimen the bridge from structural analysis
to the simulation/convergence work ([`docs/ideas.md`](../../ideas.md) #1).

---

## Questions this specimen answered

*Append here as it happens: the date, the question, and what made it click. This
is the half that cannot be regenerated, and it is what tells a later session
where Doug actually is. See `docs/ideas.md` #41.*

_(none recorded yet)_
